server:
  port: 8086

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medishop?useSSL=false
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  main:
    banner-mode: off

logging:
  level:
    root: warn
